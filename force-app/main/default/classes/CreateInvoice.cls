public with sharing class CreateInvoice 
{
    public static void GetInvoiceDetails(string customerId,string status, String invoicedate)
    {
        System.debug('I will insert the invoice record');
    }
}